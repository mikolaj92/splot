# Fala integration example

JSON stdin shape for a Fala subprocess effector calling Splot.

- `stdin.json` — sample step input  
- `fala-package.toml` — Fala package stub (subprocess effector)

Run (with Mojo Splot smoke equivalent):

```bash
./tools/mojo_run.sh mojo/smoke/fala_stdio.mojo
```
