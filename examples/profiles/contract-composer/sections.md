# Sections

This is a generic slot-composition **profile sketch**. Labels look document-like
only so the example is readable. Splot remains domain-agnostic: section names
are just candidate/signal identifiers, not a document engine.

True multi-stream **compose** (one outgoing communicate from many inputs) is
design intent; 0.3.x product focus is `select_one` fusion. See
`docs/CONCEPTUAL_MODEL.md`.
