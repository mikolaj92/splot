# Objective

Commit one stable active stream among several partial sources (e.g. cameras).

This profile is a **generic multi-stream selection** example. Splot does not
inspect pixels or video. Whatever scored each candidate — detector, LLM,
heuristic, or random — already wrote signal fields into the payload before the
round.
